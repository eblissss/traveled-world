import { motion } from 'framer-motion';
import { containerVariants, cardVariants, dropdownVariants, markerVariants } from '../lib/animations';

export { containerVariants, cardVariants, dropdownVariants, markerVariants };

export const MotionContainer = motion.div;
export const MotionCard = motion.div;
export const MotionDropdown = motion.div;

